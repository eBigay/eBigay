import CardsMenu from "../components/CardsMenu";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <div className="fade-in">
      <Hero />
      <CardsMenu />
    </div>
  );
};

export default Home;
