import Categories from "../components/Categories";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <div className="fade-in">
      <Hero />
      <Categories />
    </div>
  );
};

export default Home;
